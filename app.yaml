runtime: nodejs18

env: standard

instance_class: F2

automatic_scaling:
  min_instances: 1
  max_instances: 10

env_variables:
  NODE_ENV: "production"
  PORT: "8080"
  MONGODB_URI: "mongodb+srv://erpawan459_db_user:alhFC6pCako72e3n@helpdesk-cluster.hlw9wff.mongodb.net/portfoliobuilder"
  JWT_SECRET: "your-super-secret-jwt-key-change-this-in-production"
  CORS_ORIGIN: "*"
  STORAGE_TYPE: "local"
  UPLOAD_DIR: "./uploads"

handlers:
- url: /.*
  script: auto
  secure: always
